const createPathTabs = (parsedData) => { console.log("createPathTabs called"); const availableTopics = Object.keys(parsedData.messages); const pathTopics = ["local_position_est", "vehicle_local_position", "visual_odom"]; const availablePathTopics = pathTopics.filter(topic => availableTopics.includes(topic)); if (availablePathTopics.length === 0) return; const tab2d = { id: "2d-path", name: "2D Path", selectedTopics: availablePathTopics.map(topic => `${topic}.x`).concat(availablePathTopics.map(topic => `${topic}.y`)), plotData: [], plotLayout: {}, timeRange: [0, 100], layoutColumns: 1, plots: [] }; setTabs(prev => [...prev.filter(tab => tab.id !== "2d-path"), tab2d]); setActiveTabId("2d-path"); };
